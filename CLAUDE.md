# Project Context & Architecture
- **Tech Stack:** TypeScript
- **Frameworks & Libraries:** Svelte
- **Primary Goal:** Build a frontend for a visual note-taking app based on draggable notes on a virtual whiteboard.

# Coding Standards & Guidelines
- Follow functional programming principles where applicable.
- Use `camelCase` for variables and `PascalCase` for components.
- Include JSDoc comments for all public functions.

# Task & Workflow Rules
- **Testing:** Always write tests using Vitest and Playwright before implementing major features.
- **File Edits:** Only edit files in the `/src` and `/tests` directories unless specifically instructed otherwise.
- **Constraints:** Never delete or modify files in the `node_modules` or `.git` folders.
- **Verification:** Before claiming a task is complete, run the linter and test suite.

# Communication Style
- Be concise. Skip pleasantries (e.g., "Certainly!", "I can help with that").
- Directly show code snippets, then provide a brief explanation.
- If you encounter an ambiguous requirement, ask for clarification before
